<p style="color: green"><%= notice %></p>

<h1><%= t("index.posts.title") %></h1>

<div id="posts">
  <% @posts.each do |post| %>
    <%= render post %>
    <p>
      <%= link_to t("index.posts.show"), post %>
    </p>
  <% end %>
</div>

<%= link_to t("index.posts.new"), new_post_path %>
